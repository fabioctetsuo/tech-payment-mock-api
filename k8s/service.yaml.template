apiVersion: v1
kind: Service
metadata:
  name: payment-mock-service
  namespace: payment-mock-service
  labels:
    app: pagamento-mock
spec:
  type: ClusterIP
  ports:
  - port: 4000
    targetPort: 4000
    protocol: TCP
    name: http
  selector:
    app: pagamento-mock 